---
const { title } = Astro.props
---
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Sorpresa amor 💙</title>
  </head>
  <body>
    <main class="main-app">
      <slot />
    </main>
  </body>
</html>

<style is:global>
  :root {
    --color-primary: #fb0f85;
    --color-primary-darken: #fd6fb6;
    --color-secondary: #e8e7e6;
    --color-secondary-darken: #8f8b86;
    --color-black: #000000;
    --color-white: #ffffff;
    --gap-default: 1rem;
    --gap-extended: 2rem;
  }

  body {
    margin: 0;
    padding: 0;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto",
      "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans",
      "Helvetica Neue", sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    background-color: var(--color-secondary);
  }

  h1 {
    padding: 0;
    margin: 0;
  }

  .main-app {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    height: 100vh;
    gap: var(--gap-default);
  }


  @media (width >= 768px) {
    .main-app {
      flex-direction: row;
    }
  }
</style>