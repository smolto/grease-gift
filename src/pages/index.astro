---
import Card from "./../components/Card/Card.astro";
import Layout from "./../layouts/Layout.astro";
---

<Layout title="Sorpresa 💙">
  <div class="main-app--gift-image-container">
    <img
      class="main-app--gift-image"
      src="/opened-gift-box.png"
      alt="Una caja de regalos abierta"
    />
  </div>
  <div class="main-app--gift-presentation-container">
    <h1>
      Descubre tu <span class="gift-presentation--title-highlighted">sorpresa</span>
    </h1>
    <p class="gift-presentation--paragraph">
      Hola cariño, espero haberte sacado una sonrisa 🥰. Disfruta mucho de la
      sorpresa porque te lo mereces. Eres el amor de mi vida y mi angelito tenía
      que tener otra sorpresa más por su santo. Te amo muchísimo pequeñaja! 😍💙
    </p>
    <a class="navigation-item" href="/clues/">¿Vamos?</a>
  </div>
</Layout>

<style>

  .welcome-user-name {
    color: var(--color-primary);
    font-weight: bold;
  }

  .main-app--gift-image-container {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .main-app--gift-image {
    width: 300px;
  }

  .main-app--gift-presentation-container {
    display: flex;
    flex-direction: column;
    margin: 0 var(--gap-default);
  }

  .gift-presentation--title-highlighted {
    color: var(--color-primary);
    font-weight: 900;
  }

  .navigation-item {
    height: 45px;
    width: 150px;
    background-color: var(--color-primary);
    border: 1px solid var(--color-primary);
    color: var(--color-secondary);
    border-radius: 7px;
    -webkit-transition: all ease 0.4s;
    -moz-transition: all ease 0.4s;
    transition: all ease 0.4s;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    color: var(--color-white);
    display: flex;
    justify-content: center;
    align-items: center;
    text-decoration: none;
  }

  .navigation-item:hover {
    box-shadow: inset 150px 0 0 0 var(--color-primary-darken);
    border: 1px solid var(--color-primary-darken);
    color: var(--color-white);
  }

  .gift-presentation--paragraph {
    text-align: justify;
    text-justify: inter-word;
  }

  @media (width >= 768px) {
    .main-app--gift-image {
      width: 700px;
    }

    .main-app {
      flex-direction: row;
    }
  }
</style>
